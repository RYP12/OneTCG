{% extends 'base.html.twig' %}

{% block title %}Ranking
{% endblock %}

{% block body %}
	{# SECCIÓN: Detalle de Ranking #}
	<h1>Ranking</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ ranking.id }}</td>
			</tr>
			<tr>
				<th>Nombre</th>
				<td>{{ ranking.nombre }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_ranking_index') }}">back to list</a>

	<a href="{{ path('app_admin_ranking_edit', {'id': ranking.id}) }}">edit</a>

	{{ include('admin_ranking/formularioBorradoRanking.html.twig') }}
{% endblock %}
